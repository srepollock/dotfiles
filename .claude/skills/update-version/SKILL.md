---
name: update-version
description: Updates the projet version.
---

Updates the project version. This changing all version references in package files (ie: package.json, project.toml, etc) and public facing versions (ie: in the frontend). The version number should follow the semantic versioning method: {major}.{minor}.{patch}. Please reference the commit history which is in the conventional commits style to generate the version number.
