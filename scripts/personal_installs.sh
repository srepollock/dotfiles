#!/usr/bin/bash

# Personal installs that I like

# I will need to get the type of system to check the package managers. Also,
# each system or Linux install may have different requirements. Hopefully I
# can just setup an install though.
#
